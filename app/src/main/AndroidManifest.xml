<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trinh.library">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.ThemNguoiMuonActivity"></activity>
        <activity android:name=".View.ThemSachActivity" />
        <activity android:name=".View.ThongtinActivity" />
        <activity android:name=".View.DSNguoiMuonActivity" />
        <activity android:name=".View.QuyDinhActivity" />
        <activity android:name=".View.DSSachActivity" />
        <activity android:name=".View.HomeActivity" />
        <activity android:name=".View.DoiMatKhauActivity" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.DangKyActivity" />
        <activity android:name=".View.DangNhapActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>